package connection;

public class Decryptor {
    public static void decryptMessage(Object message) {
        /*String[] words = message.split(" ");
        switch (words[0]) {
            case "join":
                Game.addPlayer(new Player(words[1]));
        }
        showBoard();*/
    }
}
